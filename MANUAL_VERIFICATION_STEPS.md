# KaTeX 公式渲染 — 手动浏览器验证步骤

**预计耗时**：5-10 分钟  
**需要工具**：浏览器（Chrome / Firefox / Safari）  
**不需要**：任何扩展或命令行

---

## 📋 完整验证清单

### ✅ 第 1 步：打开应用

```
操作：用浏览器访问 http://149.248.16.187:8080
预期：看到登录界面
     - 标题：「众感 Sensemble · 开放感知数据平台」
     - 登录表单可见
     - 无错误提示
     
验证：☐ 页面加载 < 3 秒
     ☐ 看到登录界面
     ☐ 无红色错误
```

---

### ✅ 第 2 步：登录

```
操作：输入登录信息
     用户名：sim_teacher
     密码：sim123456
     点击「登录」

预期：重定向到仪表盘，显示概览页面

验证：☐ 登录成功（无 401/403 错误）
     ☐ 重定向到 #/dashboard
     ☐ 看到欢迎信息「欢迎 sim_teacher」
     ☐ 左侧导航菜单可见
```

---

### ✅ 第 3 步：导航到教学中心

```
操作：点击左侧导航菜单「教学中心」

预期：左侧显示知识点树，右侧显示内容区

验证：☐ 左侧知识点树加载（< 1 秒）
     ☐ 看到至少 1 个知识点
     ☐ 右侧初始提示：「← 从左侧选择一个知识点开始学习」
```

---

### ✅ 第 4 步：选择包含公式的知识点

```
操作：在左侧知识点列表中，选择一个知识点
     （推荐：最新创建的「集成测试知识点」或「KaTeX 测试」）

预期：右侧显示知识点详情

验证：☐ 知识点标题显示
     ☐ 知识点内容加载（< 1 秒）
     ☐ 可看到完整的 HTML 内容（标题、段落、列表等）
```

---

### ✅ 第 5 步：核心验证 - 检查公式渲染（⭐ 最重要）

```
操作：在右侧内容区，查找数学公式

关键点 - 您应该看到的是什么：

✅ 正确的样子：
   ┌─────────────────────────────────────┐
   │ 阻值随温度变化的关系式为：          │
   │                                      │
   │    R(T) = R₀(1 + αT + βT²)         │← 数学符号
   │                                      │
   │ 其中：                              │
   │ • R₀ 是 0℃ 时的阻值                 │
   │ • α 和 β 是温度系数                  │
   └─────────────────────────────────────┘

❌ 错误的样子（表示未渲染）：
   ┌─────────────────────────────────────┐
   │ 阻值随温度变化的关系式为：          │
   │                                      │
   │  $$R(T) = R_0(1 + \alpha T + ...$$  │← 原始 LaTeX 代码
   │                                      │
   └─────────────────────────────────────┘

验证清单：
☐ 看到数学符号（不是 $$...$$）
☐ 公式清晰可读
☐ 公式位置合理（居中或行内）
☐ 没有看到反斜杠 \ 或美元符号 $
☐ 希腊字母（α、β）正确显示
☐ 上标和下标正确显示（如 R₀ 的小字 0）
```

---

### ✅ 第 6 步：检查浏览器 Console（无错误）

```
操作：打开浏览器开发者工具
     按键：F12（Windows/Linux）或 Cmd+Option+I（Mac）
     或：右键点击 → 检查元素 → 选择「Console」标签

预期：Console 中没有红色错误信息

检查点：
1. 查看顶部是否有红色 × 符号
   ☐ 无红色 × = ✅ 成功
   ☐ 有红色 × = ⚠️ 需检查错误内容

2. 查看错误内容（如有）
   如果有红色错误，应该是：
   ✅ 可以接受的错误：
      - CORS 错误（跨域）
      - 外部服务连接失败（如分析工具）
      - 浏览器扩展错误
   
   ❌ 不能接受的错误：
      - 「Uncaught SyntaxError」
      - 「Cannot read property」（与 KaTeX 相关）
      - 「Failed to load resource」（KaTeX 文件）

验证：☐ 无关键错误
     ☐ 或只有可接受的外部错误
```

---

### ✅ 第 7 步：检查 Network 标签（字体加载）

```
操作：
1. 打开 DevTools（F12）
2. 点击「Network」标签
3. 刷新页面（Ctrl+R 或 Cmd+R）
4. 在搜索框中输入：katex
5. 观察列出的文件

预期：看到多个 KaTeX 字体文件，都是 200 OK

检查项：
│ 文件名                              │ 大小   │ 状态    │
├─────────────────────────────────────┼────────┼─────────┤
│ KaTeX_Main-Regular.woff2            │ 26 KB  │ 200 ✅  │
│ KaTeX_AMS-Regular.woff2             │ 28 KB  │ 200 ✅  │
│ KaTeX_Main-Regular.woff             │ 30 KB  │ 200 ✅  │
│ ... 其他 KaTeX 文件                 │   ...  │ 200 ✅  │
└─────────────────────────────────────┴────────┴─────────┘

验证：☐ 至少看到 10+ 个 KaTeX 字体文件
     ☐ 所有文件状态都是 200（绿色）
     ☐ 文件大小正常（1-50 KB）
     ☐ 无 404（红色）或超时错误
```

---

## 📝 记录验证结果

```
【KaTeX 公式渲染 - 浏览器验证报告】

日期：________________
时间：________________
浏览器：Chrome / Firefox / Safari / 其他 ________

步骤 1：访问应用          ☐ 通过 / ☐ 失败
步骤 2：登录              ☐ 通过 / ☐ 失败
步骤 3：导航教学中心      ☐ 通过 / ☐ 失败
步骤 4：选择知识点        ☐ 通过 / ☐ 失败

步骤 5：公式渲染检查      ☐ 通过 / ☐ 失败
   - 看到数学符号         ☐ 是 / ☐ 否
   - 公式清晰             ☐ 是 / ☐ 否
   - 希腊字母正确         ☐ 是 / ☐ 否
   - 上下标正确           ☐ 是 / ☐ 否

步骤 6：Console 无错误    ☐ 通过 / ☐ 失败
   - 无红色错误           ☐ 是 / ☐ 否
   - 无 KaTeX 相关错误    ☐ 是 / ☐ 否

步骤 7：字体加载验证      ☐ 通过 / ☐ 失败
   - 看到 10+ KaTeX 文件  ☐ 是 / ☐ 否
   - 所有文件 200 OK      ☐ 是 / ☐ 否

════════════════════════════════════════════
总体结果：☐ 全部通过 / ☐ 部分通过 / ☐ 失败

如果全部通过，恭喜！KaTeX 公式渲染 ✅ 成功！
```

---

## 🆘 如果验证失败怎么办？

### 场景 1：看不到数学符号，只看到原始 LaTeX 代码

**症状**：
```
看到：$$R(T) = R_0(1 + \alpha T + \beta T^2)$$
应该看到：R(T) = R₀(1 + αT + βT²)
```

**可能原因**：
1. ❌ 公式未渲染（JavaScript 未执行）
2. ❌ KaTeX 库未加载
3. ❌ 浏览器缓存问题

**解决方案**：
```
1. 硬刷新：Ctrl+Shift+R（Windows）或 Cmd+Shift+R（Mac）
2. 等待 2 秒，让 JavaScript 异步渲染
3. 如果仍未显示，检查 Console 是否有错误
4. 如果有「Cannot find module 'katex'」错误，联系开发团队
```

---

### 场景 2：Console 中有红色错误

**症状**：按 F12 → Console 看到红色错误信息

**分类**：
- ✅ 可接受：跨域 CORS、第三方服务、浏览器扩展
- ❌ 不可接受：KaTeX、主应用 JavaScript

**解决方案**：
```
1. 复制错误信息
2. 搜索关键词：
   - 如果包含 "katex" → KaTeX 相关问题
   - 如果包含 "vue" / "Teaching" → Vue 组件问题
3. 将错误信息报告给开发团队
```

---

### 场景 3：字体文件加载失败（404）

**症状**：Network 标签中 KaTeX 文件显示 404 或 503

**原因**：字体文件未部署或路径错误

**解决方案**：
```bash
# 在服务器检查文件
docker exec sensemble-platform-frontend-1 \
  ls -lah /usr/share/nginx/html/assets/ | grep -i katex

# 如果文件不存在，重新构建前端
cd /opt/sensemble-platform
docker compose -f docker-compose.prod.yml build --no-cache frontend
docker compose -f docker-compose.prod.yml restart frontend
```

---

## ✅ 成功标志

如果您看到以下现象，就是 **✅ 成功**：

1. 📖 **公式显示为数学符号**（不是代码）
2. 📊 **公式排版专业**（有合适的字号和间距）
3. 🔤 **希腊字母正确**（α、β 等）
4. 🎯 **上下标正确**（R₀、T²）
5. 💻 **Console 无关键错误**
6. 📥 **所有字体文件加载成功**（200 OK）

---

## 📞 如果仍需帮助

1. **保存截图**：
   - 公式显示的内容区
   - Console 标签的错误信息
   - Network 标签的字体加载情况

2. **收集信息**：
   - 浏览器和版本
   - 操作系统
   - 错误信息全文

3. **联系开发团队**：
   - 提交截图和错误信息
   - 参考本文档的「场景」部分定位问题

---

**祝验证顺利！** ✨
