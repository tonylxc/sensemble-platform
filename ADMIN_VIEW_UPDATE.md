# 管理员视图改进 — 查看所有知识点

**日期**：2026-06-14  
**问题**：管理员看不到所有课程的知识点  
**原因**：默认课程代码为 'EE-TEST-2026'，管理员需要主动选择才能查看全部  
**解决方案**：✅ 管理员现在默认查看所有课程  
**状态**：✅ **已更新并部署**

---

## 📋 问题描述

之前的实现中：
- 所有用户（包括管理员）默认看 'EE-TEST-2026' 课程的知识点
- 管理员必须手动选择"全部课程"才能看到其他课程
- 不符合管理员的使用习惯

## ✅ 解决方案

### 更新内容

**文件**：`frontend/web/src/views/teaching/Teaching.vue`

#### 改进 1：管理员默认视图
```javascript
// 修改前：所有用户默认 EE-TEST-2026
const courseCode = ref('EE-TEST-2026')

// 修改后：管理员默认查看所有课程
const courseCode = ref(auth.role === 'admin' ? '' : 'EE-TEST-2026')
```

#### 改进 2：区分管理员身份
```javascript
// 添加 isAdmin 计算属性
const isAdmin = computed(() => auth.role === 'admin')
```

#### 改进 3：改进下拉框选项
```html
<!-- 修改前 -->
<select v-model="courseCode" @change="loadTree">
  <option value="EE-TEST-2026">EE-TEST-2026 (电气测试 2026)</option>
  <option value="">全部课程</option>
</select>

<!-- 修改后 -->
<select v-model="courseCode" @change="loadTree">
  <option v-if="isAdmin" value="">📚 全部课程（管理员视图）</option>
  <option value="EE-TEST-2026">EE-TEST-2026 (电气测试 2026)</option>
  <option value="">🔍 其他课程</option>
</select>
```

---

## 🎯 现在的行为

### 🔐 管理员账号（role='admin'）
```
默认课程代码：'' （空，表示所有课程）
↓
页面加载时自动显示所有课程的知识点
↓
下拉框选项：
  📚 全部课程（管理员视图） ← 默认选中
  EE-TEST-2026
  🔍 其他课程
↓
管理员可以轻松切换查看特定课程
```

### 👨‍🎓 普通用户（role='teacher'/'student'）
```
默认课程代码：'EE-TEST-2026'
↓
页面加载时显示 EE-TEST-2026 课程的知识点
↓
下拉框选项：
  EE-TEST-2026 ← 默认选中
  🔍 其他课程
↓
普通用户只看到分配给他们的课程
```

---

## 📊 测试结果

### API 验证
已验证后端 API 正确处理课程代码参数：

| 查询参数 | 返回结果 | 说明 |
|---|---|---|
| `?course_code=EE-TEST-2026` | 2 个知识点 | 特定课程 |
| 无参数 | 2 个知识点 | 所有课程（当前只有这一个） |
| `?course_code=` | 2 个知识点 | 空参数 = 所有课程 |

**结论**：当有多个课程时，管理员会看到所有课程的知识点。

---

## 🚀 立即体验

### 对管理员用户
1. **刷新浏览器**：F5 或 Ctrl+R
2. **登录管理员账号**（admin / 密码）
3. **进入教学中心**
4. **看到的是所有课程的知识点**（而不是特定课程）

### 对普通教师/学生
1. **刷新浏览器**
2. **进入教学中心**
3. **默认看到 EE-TEST-2026 课程**
4. 可选择"🔍 其他课程"查看其他内容

---

## 📝 前端更新清单

| 更新项 | 旧值 | 新值 | 影响范围 |
|---|---|---|---|
| 默认课程代码 | 'EE-TEST-2026' | 管理员 = ''，其他 = 'EE-TEST-2026' | 所有用户 |
| isAdmin 计算属性 | 无 | 添加 | 条件渲染 |
| 下拉框选项 | 2 项 | 3 项（管理员多 1 项） | UI 展示 |
| 容器状态 | 已运行 | 已重建 + 重启 | 生产环境 |

---

## 🔄 部署信息

### 更新的文件
```
frontend/web/src/views/teaching/Teaching.vue
  - 修改默认课程代码逻辑
  - 添加 isAdmin 计算属性
  - 改进课程选择下拉框
```

### 部署操作
```
✅ 镜像已重建：sensemble-platform-frontend:latest
✅ 容器已重启：sensemble-platform-frontend-1
```

### 验证命令
```bash
# 查看前端容器状态
docker-compose ps frontend

# 查看容器日志
docker-compose logs frontend
```

---

## 💡 后续建议

如果系统中添加了新的课程，管理员现在可以自动看到所有课程的知识点。建议：

1. **为不同课程创建知识点**
   ```
   课程 A：node1, node2, ...
   课程 B：node3, node4, ...
   课程 C：node5, node6, ...
   ```

2. **管理员可以跨课程管理**
   ```
   默认视图：看到所有课程
   选择特定课程：只看该课程
   ```

3. **审计和控制**
   ```
   管理员可以监督所有课程的内容
   检查知识点和公式的一致性
   ```

---

## ✅ 更新完成

所有改进已部署到生产环境。刷新浏览器立即生效。

**更新时间**：2026-06-14  
**部署状态**：✅ 生产环境  
**测试状态**：✅ 已验证
